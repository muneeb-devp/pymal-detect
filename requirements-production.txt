# Production-only dependencies for deployment (minimal)
# Only includes what's needed for inference

# Core dependencies
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.6.0

# Only the model library actually used (LightGBM)
lightgbm>=4.6.0

# Web framework
flask>=3.1.0
flask-cors>=4.0.0
gunicorn>=23.0.0

# Utilities
joblib>=1.4.0
